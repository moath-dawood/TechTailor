import { BrowserRouter, Route, Routes } from "react-router-dom";

export default function Router() {
    <BrowserRouter>
        <Routes>
            <Route path="/" >

            </Route>

        </Routes>
    </BrowserRouter>
}